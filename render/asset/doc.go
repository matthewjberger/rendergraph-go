// Package asset loads and builds the resources the renderer draws: glTF
// import, meshes (including skinned and instanced variants), materials,
// textures, and skeletal animation clips with their playback state.
package asset
