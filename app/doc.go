// Package app is the engine entry point. An App holds the lifecycle
// callbacks a game supplies (initialize the world, configure the render
// graph, run systems, pre-render) and drives the per-frame loop that steps
// the ECS world and submits the renderer's graph.
package app
